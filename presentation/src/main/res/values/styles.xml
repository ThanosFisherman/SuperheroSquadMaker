<resources>
    <!-- Base application theme. -->

    <!-- Base application theme. -->
    <style name="Theme.SquadMaker" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/deep_purple_500</item>
        <item name="colorPrimaryDark">@color/deep_purple_700</item>
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
        <item name="colorError">@color/red_A700</item>
        <item name="android:textColor">@color/grey_800</item>
        <item name="android:windowBackground">@color/grey_50</item>
        <item name="android:windowEnableSplitTouch">false</item>
        <item name="android:splitMotionEvents">false</item>
        <!--        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearance.SonicoLife.LargeComponent</item>-->
    </style>

    <!-- The launcher theme. It sets the main window background to the launch_screen drawable -->
    <style name="Theme.SquadMaker.LaunchScreen" parent="Theme.MaterialComponents.Light.NoActionBar">

        <item name="android:windowBackground">@drawable/splash_background</item>

        <!-- Optional, on Android 5+ you can modify the colorPrimaryDark color to match the windowBackground color for further branding-->
        <item name="colorPrimaryDark">@color/deep_purple_700</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>


    <style name="Theme.SquadMaker.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <style name="Theme.SquadMaker.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <style name="myDialogAnimations">
        <item name="android:windowEnterAnimation">@anim/slide_in_from_left</item>
        <item name="android:windowExitAnimation">@anim/slide_out_to_right</item>
    </style>

</resources>